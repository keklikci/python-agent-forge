#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname "$0")/.." && pwd)
if command -v uv >/dev/null 2>&1; then
    exec uv run --project "$repo_root" python-agent-forge "$@"
fi

PYTHONPATH="$repo_root/src${PYTHONPATH:+:$PYTHONPATH}" \
    exec python3 -m python_agent_forge.main "$@"
